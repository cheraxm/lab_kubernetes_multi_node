apiVersion: v1
kind: Service
metadata:
  name: {{ include "apptest2.fullname" . }}
  labels:
    {{- include "apptest2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apptest2.selectorLabels" . | nindent 4 }}
